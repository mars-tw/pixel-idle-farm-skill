# Codex 回應 — 手機端 P0 修正

目標版本：`r55-20260712-1`

## 結論

- **M-P0-01／02 已修**：真實 touch 點農土時，首點只選取高亮並顯示動作預覽；第二次點同一格，且工具、種子、動作仍一致，才會移動並執行種植／澆水／收成。改點相鄰格只會改選取，不扣資源。
- **桌面行為不變**：mouse 與既有程式化操作維持單擊直接執行。
- **命中辨識改善**：農土加入 2px 視覺間距，待確認格有獨立高亮；預覽會顯示種植作物與種子成本，或澆水／收成動作。
- **觸控 guard 已補**：覆蓋首點零扣款、改點相鄰格零扣款、同格第二點才執行、mouse 單擊直達；靜態 guard 另鎖同格／同工具／同種子／同動作條件。

## P1／P2

- **已修背景節流**：`document.hidden` 時主 loop 直接返回、autosave 不重複寫入；進背景先存一次，回前景立刻刷新。FPS 監測跳過 hidden 與回前景第一幀，避免背景幀距誤觸 auto-low。
- **已修 low 殘留動畫**：low 模式停止成熟點與任務標記的無限動畫。
- **本輪延後**：手機地圖／側欄一級切換、點磚自動捲動、`updateMap` dirty-layer 重構、pan／zoom。自動捲動會打斷本輪「同格第二點確認」，DOM 分層則屬高風險渲染重構，留待獨立輪次。

## 邊界與驗證

- `src/config.js`、`src/game.js`、`src/state.js` 無差異；規則與經濟未改。
- 版本已同步至 `r55-20260712-1`，舊完整版本字串 grep 為 0。
- `npm test`：綠。
- `npm run test:e2e`：連續 3 輪綠；每輪含 RPG 長鏈、真 SW 離線、觸控確認 guard、RWD 9 視口 × overlay 開／關。
- 未執行 git commit／push。
